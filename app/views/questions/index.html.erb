<hr>
<%= link_to 'Create', new_test_question_path %>
<hr>
<% @test.questions.each do |question| %>
  <p>
    <%= question.body %>
    <%= link_to 'Delete', question_path(question), method: :delete  %>
  </p>

<% end %>
<hr>

