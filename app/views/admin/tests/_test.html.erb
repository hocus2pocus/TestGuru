    <tr>
      <td><%= test.title %></td>
      <td><%= test_level(test) %></td>
      <td><%= test.questions.count %></td>
      <td><%= link_to t('layouts.admin.show'), admin_test_path(test) %></td>
      <td><%= link_to t('layouts.admin.edit'), edit_admin_test_path(test) %></td>
      <td><%= link_to t('layouts.admin.delete'),
                      admin_test_path(test),
                      method: :delete,
                      data: { confirm: 'Are you sure?' } %>
      </td>
    </tr>
